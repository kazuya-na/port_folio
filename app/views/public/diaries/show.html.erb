<div class="container p-5">
  <div class="row">
    <div class="col">
      <h1>日誌詳細</h1>
      <div><%= link_to '編集', edit_diary_path(@diary.id), class: 'btn btn-success' %></div>
      日付：<%= @diary.date %><br>
      血圧：<%= @diary.dbp %> / <%= @diary.sbp %> mmHg<br>
      体温：<%= @diary.bt %> ℃<br>
      体重：<%= @diary.weight %> kg<br>
      運動：<%= @diary.exercise %>
    </div>
  </div>
</div>